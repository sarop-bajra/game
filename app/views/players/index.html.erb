<h3>Players</h3>

<ul>
      <% @players.each do |p| %>
      <li>
        <a href="<%= player_path(p.id) %>">
        <%= image_tag p.image, alt: p.name%>
        </a>
      </li>
      <% end %>
</ul>
